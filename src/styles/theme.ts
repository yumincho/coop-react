import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    primary: {
      light: '#4caf50',
      main: '#087f23',
      dark: '#004d00',
    },
    secondary: {
      light: '#f73378',
      main: '#f50057',
      dark: '#ab003c',
    },
  },
})

export default theme
